"use client";

import { useMemo } from "react";

export default function useTheme() {
    // En attendant une gestion dynamique du thème, on retourne la couleur émeraude par défaut
    return "#10b981";
}
